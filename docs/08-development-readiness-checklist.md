# 开发就绪清单

> 状态：用户已批准开始开发；V9.0 参数已确认，当前按阶段验收节点实施。

## 1. 已具备

- [x] 产品目标、非目标、核心流程和MVP验收已定义。
- [x] V8.1–V8.6已在设计上统一为V9.0单一规则入口。
- [x] 全A股扫描、领域配额、跨领域席位和科创默认禁入已明确。
- [x] 免费数据源、时点、复权、质量和降级原则已明确。
- [x] AI权限、结构化输出、自我验证、记忆和升级流程已明确。
- [x] 规则、AI、人工三版本对照与评估指标已明确。
- [x] 本机到云端的目标架构已明确。
- [x] Web信息层级、页面和核心交互已明确。
- [x] API与数据库边界已形成初稿。
- [x] 项目级AGENTS.md已生效。
- [x] 每日主要标的简报的范围、不可换股边界和降级原则已确认。
- [x] 登录、管理员完整权限、管理员新增普通用户及普通用户只读边界已确认并实现后端基线。
- [x] 中上协行业底座与中证五类主题样本已接入，唯一主领域带有效期并拒绝同级冲突。

## 2. 开发启动门槛

开始写业务源码前必须满足：

- [x] 用户已于 2026-08-03 明确发出开始开发授权。
- [x] MVP范围已纳入每日主要标的简报，没有新增阻塞性需求。
- [x] 已把旧 `pick_a_weekly` 规则和历史产出作为只读迁移源盘点，不让PAWE运行时依赖旧目录。
- [x] 已对腾讯前复权K线做两只不同交易所股票的最小探测并记录结果；其他数据源仍按实现阶段继续探测。
- [x] 首个AI提供商采用 OpenAI Responses API，开发默认 `gpt-5.6-sol`；同时准备确定性Mock，避免开发被密钥阻塞。
- [x] 已创建脚手架并补充根README中的实际安装、启动、检查、测试和构建命令。

## 3. 建议实施顺序

1. 建立工程、契约Schema和测试框架。
2. 建立股票主数据、交易日历、日线适配器和冻结快照。
3. 实现确定性特征与V9.0规则基线。
4. 迁移历史周度资料，建立walk-forward回放。
5. 实现AI事实底稿、结构化调整和审计降级。
6. 实现三版本决策、人工审批和名单冻结。
7. 实现收盘后每日简报、证据更新和 AI 降级。
8. 实现周终复盘、错误归因和实验记录。
9. 接入登录、管理员用户管理和只读权限，构建低信息密度Web页面并完成浏览器验收。
10. Docker化本机运行；稳定后进入实时影子期。
11. 达到自动化与运维门槛后再设计云部署。

新增能力按两批实施，避免实验基础设施越权影响现有正式链路：

1. 第一批（设计契约）：统一“目标且最多 5 只”、逐标的约 10% 研究情景、多目标评估、受限规则 DSL、数据源能力矩阵、特征产物清单、实验状态机、API/数据库草案、界面和测试门禁。
2. 第二批（实现与迁移）：实现 Schema/数据库迁移、白名单 DSL 校验器、能力注册表、Parquet/DuckDB 加速层、walk-forward 折叠运行器、影子与审批/回滚服务、API 和管理界面，再补齐单元、集成、回放与浏览器验收。

第一批文档契约已完成。第二批已完成旁路后端基线：数据库迁移 `20260813_0015`、任务取消检查点迁移 `20260813_0016`、白名单 DSL 静态校验器、实验状态机与乐观版本锁、walk-forward 折叠契约、数据源能力注册表、特征产物清单、PyArrow Parquet 原子写入、DuckDB 白名单只读查询、治理 API，以及只读“规则实验”管理页面。实验激活默认关闭，尚未建立真实实验提案、批量回放执行 worker、持续影子周采集和正式规则激活指针，因此不得称为自动进化已经上线。

当前新增能力保持现有 V9.0 正式规则和发布链默认行为不变，以新增表和接口旁路接入；PostgreSQL 仍是正式快照与特征的权威来源，尚无正式 Parquet 分区接管规则输入。

当前第 4 步已完成旧 Markdown 隔离暂存、370 条具备声明基准的主池记录单源复算、42 条冲突的证据型归因，以及 74 份周终结果的整周原子资格清单。研究性完整结果为默认臂20周、新规则臂6周、旧规则臂5周；全部仍为单源，正式回放可用数为0。选择时点快照、历史分类和备用源验证尚未齐备，因此 walk-forward 正式运行尚未开始，该步骤整体仍在进行中。

本机 Web 已改为只展示数据库正式发布名单，并完成管理员决策审批/发布界面、V9周初任务幂等入口和任务历史恢复。数据库已升级至 `20260810_0013`：新增隔离的历史周回放、周终复盘和逐标的评价表；正式发布账本不受研究回放影响。2026-08-09 沪深主数据 `5206` 只已原子写入，北交所按用户确认暂从完整性硬要求中剔除并显式标记降级；正式分类范围为 `463` 只活跃沪深股票，其中科创板 `120` 只不参与本次完整性门禁。其余 `343/343` 只技术特征全部成功。后台已形成“周日 18:00 数据准备 → 工作日 08:30 规则任务 → 交易日 15:30 数据拉取与日报单任务 → 最后交易日日报齐备后周终复盘”链。复盘只补足规则/AI/发布版本差异标的、行业样本和沪深300基准所需数据。历史数据页按周列出，点击后在周级弹框查看正式或完整回放内容。2026-08-03 周真实历史回放严格记录周初、逐日和周终信息边界，V9 硬约束后实际入选 2 只并完成 5 份简报、沪深300及行业超额评价。公告、财务、催化和 AI 调整仍待完成。

个人自选基线已加入 `20260812_0014`：管理员和普通用户都只能管理本人最多 5 只沪深 A 股，生效日、移除时点、日报和周终结果独立保存；自动或人工日报/周终任务会在同一批次同步生成本人自选内容，且数据库与任务逻辑均不把自选接入公共决策集。首页“我的自选”下设独立的“每日简报”和“每周复盘”入口，公共日报及历史归档不混入个人内容。

新增任务进度与审计中心：手动周初、当日日报和当前周周终复盘均先持久化为排队状态，后台执行器复用正式自动任务逻辑并逐阶段更新；前端自动刷新、展示事件时间线和历史运行记录，刷新后仍恢复最新持久化状态。同一周只允许一个排队或运行中的周初任务，同类型产出任务活动期间复用已有任务。研究驾驶舱已支持按需展开每支标的的冻结规则理由和评分构成、六类市场状态、三级组合置信度及决策版本的审计意义；主页统一页面宽度，并将日报收敛为观察名单下方的日期入口与详情弹窗。

## 4. 开发阶段验收节点

| 节点 | 通过条件 |
|---|---|
| 数据基线 | 快照时点、复权、交易日和主备降级测试通过 |
| 规则基线 | 固定样例覆盖全部V9.0状态，结果可解释可重放 |
| AI基线 | Schema、证据、权限、换入上限和降级测试通过 |
| 决策闭环 | 三版本、人工审批、冻结、周终归因完整 |
| Web MVP | 桌面/移动真实浏览器主流程与异常状态通过 |
| 本机候选版 | Docker可重复启动，备份和数据清理边界明确 |
| 自动发布候选 | 至少12个完整交易周满足评估规范门槛 |

### 新增能力验证矩阵

- [ ] 组合容量覆盖 5/4/3/2/1/0 只；1～5 只满足全部约束，0 只停止发布，超过 5 只和不合格补位被拒绝。
- [ ] 每个正式决策项都包含约 10% 研究情景、概率、证据、风险和失效条件，页面明确非收益保证。
- [ ] DSL 覆盖合法提案、未知特征、参数越界、未来标签、越权硬约束、任意代码和非法状态跳转。
- [ ] walk-forward 验证训练/选择/验证窗口隔离、折叠原子性、集合容量拆分和单一锚点敏感性。
- [ ] 影子实验无正式发布权限；批准使用版本锁，激活前重跑门禁，回滚恢复指定正式版本。
- [ ] 数据源能力矩阵覆盖正式/研究资格、条款过期、主备降级和映射版本晋级。
- [ ] 特征加速层覆盖确定性哈希、原子发布、中断恢复、有界内存重建及 PostgreSQL 权威性。
- [ ] API 覆盖幂等、冲突、权限、协作式取消和失败证据保留；错误不暴露路径、密钥或内部堆栈。
- [ ] 浏览器覆盖实验各折、规则差异、来源能力、特征进度以及桌面/移动异常状态。

## 5. 非阻塞待定项

以下内容可在对应实现阶段确认，不阻塞开发启动：

- 首个实际使用的AI模型及成本上限。
- 品牌视觉、正式域名和云服务商。
- Redis/独立任务队列是否需要。
- MCP、研报RAG、PDF专业报告和模拟持仓是否进入后续版本。
- 科创板未来开启时的额外风险参数。

## 6. 当前结论

用户已授权开始开发，并于 2026-08-03 二次确认 `09-rule-parameters-v9.md`。当前已经完成文档冲突消除、工程与依赖锁定、数据库迁移、V9.0 资格/评分/组合规则、真实沪深主数据和正式分类、版本化日线、343 只非科创正式范围冻结快照、保守 V9 周输入、规则候选与规则版持久化、审批/发布事务、登录与管理员/只读用户权限，以及 Web 登录、用户管理、下周审批入口和确定性每日简报闭环。主功能“自动准备数据、生成每周规则标的并进入人工审批”已经本机跑通；2026-08-10 首份正式发布日报已在 15:00 收盘信息边界下生成并显示 5 只标的，质量为 `single_source`，同时完成 15:30 后进程重启补跑、日线延迟时收盘报价兜底和备用接口快速降级验收。完整状态输入、非技术证据和 AI 决策继续推进。
