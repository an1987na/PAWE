# 手动每日简报补缺修复

## 原因与改动

只读检查显示 2026-09-07 周只有 09-10 日报。前端只请求当日，任务入队遇到当日产出直接复用完成记录，因此无法从入口补齐此前日期。

每日简报入口启用 `catch_up_week: true`，后台先检查同周已结束交易日并按日期补缺；已有产出跳过，不因当日已有结果跳过补缺。旧单日 API 默认行为保留。任务执行时检查正式周边界和日历完整性；取消在日期步骤之间生效，失败逐日留审计并继续处理后续日期，最终有失败则报告未完成日期。没有数据库迁移、规则或已发布名单变更。

## 验证与交付边界

- 按用户新要求，不使用 Mac Docker。小主机现有 API 镜像的独立断网容器运行针对性测试，专属代码和测试依赖目录，无正式数据库、运行密钥或正式数据挂载。
- `test_manual_daily_catchup.py`、`test_worker_scheduler.py`、`test_job_api.py`、`test_job_progress.py`：37 项通过。既有 FastAPI/httpx 弃用警告保留。
- 改动 Python 文件 Ruff 通过；三个后端源码文件 Mypy 通过。初次格式检查发现一处长行，修正后通过，没有为纯格式改动重复业务测试。
- 小主机 Node 22 隔离容器按锁文件安装依赖，前端类型检查与构建通过；与正式 Web 镜像一致设置空 `VITE_API_BASE_URL`，浏览器通过 SSH 访问小主机测试页面。
- 真实浏览器检查桌面及 390×844 移动端弹框；虚构数据验证补缺请求参数、成功提示、503 错误提示及普通用户无任务管理入口。没有点击正式任务或据此声称正式补跑完成。
- 部署需要同步 API、Worker 和 Web，无数据库迁移；停旧 Worker 后再切换，保留当前镜像和环境用于回退。无需重启小主机。实际部署结果见后续交付记录。

## 上线前复核

用户已明确授权推送 GitHub、更新小主机服务并补齐 09-07～09 日报。2026-09-10 23:12（Asia/Shanghai）重新只读检查发现 09-07、08、09、10 四份日报均为 active/published，且没有 queued/running 的人工任务。期间后台已补齐缺口；不强制重算已有正式日报。上线后使用新入口验证复用与缺口检查，以数据库及任务审计为准。

原环境的免费行情备源覆盖不足并未被此次入口修复解决；真实补跑仍严格执行数据质量和日期截止门禁。

## 正式交付（2026-09-11）

- 代码与协作规范已推送 GitHub：`1cc166f8bd7fd9ce55c575423468ec81963a7c9b`。上次因审批工具额度不足暂停于镜像构建之后，09-11 恢复检查并完成切换，没有绕过审批。
- 小主机发布目录 `/mnt/media/projects/PAWE/releases/1cc166f`；复用已验证基础镜像，仅叠加本提交应用代码及已测试 Web 构建，显式配置 Python 源码导入路径。源码哈希与本提交一致，未使用 Mac Docker。
- 实际运行 API：`sha256:6266daed7b9115c38c35f24c51424f4bd83e9d715a8aeeb2301608c91676dcd9`；Worker：`sha256:f76ed36d48b62653368e36f7457369d3c2e5d773f330db565b2bc11eeaee8f56`；Web：`sha256:fce87f1b0d7dd43a76f30f6e7f52df6f0744eaac00ae88c259af65a88f8c3d28`。API/数据库健康，公网 `/api/v1/ready` 返回 ready。
- 更新前备份：`backups/pawe-20260911T000151Z.dump`；旧镜像保留，原运行配置备份为小主机 `config/runtime-before-1cc166f.env`（0600，不入 Git）。只切换 API/Worker/Web，不重启主机、数据库或 VPS 转发，无迁移。
- 通过正式认证和 CSRF 提交 `catch_up_week=true`、目标日 09-10；任务 `2e48551f-3f26-4773-a554-0018aeb01acf` 成功，`reused=true`。09-07、08、09、10 四份日报均已存在且 ID/版本不变（均 V1）。所需缺口已由后台补齐，本次没有强制重算。09-11 日报仍按收盘后 15:30 门禁生成。
- 原浏览器会话已过期；使用现有配置账号正常 API 登录核验后退出，未修改密码或绕过权限。浏览器交互覆盖以此前小主机隔离验证为准。
