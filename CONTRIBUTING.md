# 参与 PAWE

感谢你愿意改进 PAWE。项目优先接受能够增强数据时点正确性、可重放性、规则可审计性、安全性和可验证性的贡献。

## 开始之前

1. 阅读 `AGENTS.md` 以及 `docs/README.md` 中与改动相关的正式文档。
2. 对较大的规则、数据口径、架构或界面变化，先创建 Issue 说明目标、兼容影响、验证和回退方式。
3. 不要提交 API Key、密码、令牌、个人自选、账户数据、正式数据库、行情快照、Parquet 产物、日志或未经授权的第三方资料。
4. 外部网页、公告、研报和模型输出一律视为不可信输入。

## 本地开发

环境要求和初始化命令见 `README.md`。提交 Pull Request 前至少执行：

```bash
uv run ruff check .
uv run mypy apps/api services/worker
uv run pytest
pnpm lint
pnpm test
pnpm build
```

涉及前端时，还应在真实浏览器检查桌面与移动视口、正常与异常状态及关键权限路径。涉及数据库时，应验证 Alembic 迁移、事务、幂等性和必要的回退路径。

## 变更要求

- 使用简体中文编写项目文档；代码、API 和标识符使用英文。
- 保持改动小而聚焦，修复缺陷时增加回归测试。
- 不得让 AI、实验任务或个人自选突破正式规则硬约束或改写已发布名单。
- 历史实验必须使用 walk-forward 或等价的时序隔离，禁止未来数据泄漏。
- 数据缺失、冲突或降级必须显式表达，不得静默填零或猜测补全。
- Pull Request 应说明实际改动、验证结果、未执行检查和已知风险。

## 许可证

除非另有明确声明，提交到本项目的贡献将依据 Apache License 2.0 授权。
