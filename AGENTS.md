# PAWE 项目协作规范

## 1. 项目与事实来源

PAWE（Pick A Weekly）是个人使用的 A 股 AI 周度研究系统。系统扫描全 A 股，以 AI、机器人、半导体、能源为主领域，创新药和医疗器械为补充领域；经规则筛选、AI 有限调整和人工确认后，每周发布目标且最多 5 只具有约 10% 周内最高涨幅研究情景的观察标的，并生成每日简报和周终复盘。所有结果不构成收益保证或交易指令。

项目采用 React Web、FastAPI、Worker、PostgreSQL 和可重建的 Parquet/DuckDB 分析层。正式事实按以下顺序解释：

`产品范围 → V9.0 规则 → 数据、AI 与评估契约 → 架构与 API → README 当前状态 → 源码与测试`

文档与实现冲突时，先判断是缺陷还是已批准但未同步的变更，不得静默改写正式规则。旧 `pick_a_weekly` 目录只可作为隔离迁移或研究资料，PAWE 运行时不得依赖它。

## 2. 工作方式与授权

- 默认使用简体中文沟通和编写文档；代码、API 与标识符使用英文。
- 开始任务前阅读本文件及相关文档、源码、测试和工作区状态，不凭旧对话推断本地代码、数据库、任务、远端或部署状态。
- 检查并保护现有分支与未提交改动；不得重置、覆盖、清理或暂存不属于当前任务的内容。未配置远端时不得假定存在 `origin/main`、CI 或线上版本。
- 实施前简要说明目标、范围、兼容影响、验证和回退方式。用户明确要求实现、修复或开发的，视为已授权执行范围内常规、可回退的改动，无需重复确认。
- 若需求与已确认规则或事实冲突、无法满足验收、存在明显的数据或安全风险、实现不可行，或可能产生重要副作用，应先说明依据、影响和修改建议；有更合适的方案时，同时说明主要收益、代价及必要备选方案，不得机械执行或以个人偏好替代用户决定。
- 可从项目文件确认的信息不重复询问；不影响核心意图的细节可作合理假设并说明后继续执行。问题会实质改变需求范围、业务结果、兼容性、风险或回退方式，或涉及正式规则、数据口径、架构替换、批量正式数据写入、共享数据库迁移执行、破坏性操作及外部发布时，必须经用户明确确认后执行。
- 审核、讨论、设计或状态查询只做分析；未经要求不修改代码、数据、依赖或运行环境。只读检查及隔离环境验证无需额外确认。
- 用户的新确认覆盖旧方案时，应同步消除核心文档中的冲突，不保留含义相同的多套正式规则。

当用户提出长期、跨任务且影响协作或安全的原则时，应建议是否纳入本文件；未经明确确认不得写入。一次性要求、具体实现细节、临时状态及已被替代的规则不写入本文件，重复出现也不自动沉淀。

## 3. 规则与决策边界

- V9.0 是唯一正式规则入口，优先级为：`数据有效性 > 硬约束 > 市场风险状态 > 恢复状态 > 分支验证 > 基础评分 > AI 调整`。
- 最终目标且最多发布 5 只；硬约束和组合约束后可发布 1～5 只，不用不合格标的凑数，零只时停止发布。主领域至少 3 只；补充领域与跨领域合计最多 2 只，其中跨领域最多 1 只。
- 科创板限制可配置且默认禁止进入主池。AI 不得突破科创板、ST、停牌、流动性、过热等硬约束。
- AI 只分析规则候选池，默认调分范围为 -10～+10，最多改变规则基准前 5 中的 2 个名额；失败、超时、证据不足或结构不合规时降级到纯规则结果或停止发布，不得伪造补全。
- 始终保存规则基准、AI 调整和人工发布三套结果。正式发布后当周名单不可被日报、模型或自动任务改写；个人自选不参与公共规则、评分或名单生成。
- AI 只能提出规则、提示词、权重或特征优化方案。正式升级必须经过时序隔离回放、实时影子验证、人工批准和可验证回滚，实验服务不得拥有正式发布权限。

## 4. 数据与可复现性

- 周初选择只能使用决策截止时间前已公开的数据；当周首个交易日开盘价只用于评价，严禁未来数据、事后分类和回看泄漏。
- 每项数据记录来源、交易日、公开或可用时间、实际获取时间、复权口径、版本和质量状态。缺失、冲突或降级不得静默填零、猜测或沿用旧结论。
- 免费数据源统一经适配器接入，业务规则不得依赖供应商私有字段；公开接口需具备缓存、节流、超时、重试和显式主备降级。
- 全市场抓取、正式持久化、强制重算和大规模历史回放执行前必须确认；先做小范围预览，不并行运行相同时间窗口的数据批次。
- PostgreSQL 是正式快照和清单的权威来源；Parquet/DuckDB 仅用于可重建的分析与加速，不得反向改写正式数据。规则、提示词、模型、代码、数据快照和发布结果必须版本化并可重放。
- 历史实验采用 walk-forward 或等价时序隔离，不得用同一时段同时调参与证明有效。周终复盘必须区分规则、AI 和人工贡献，并形成结构化错误归因。

## 5. 实现与安全

- 保持数据、特征、规则、AI、组合决策、发布、简报、复盘、实验和 Web 展示边界清晰；复杂流程使用显式状态机，不堆叠零散特例。
- API 使用结构化 Schema，服务端校验类型、范围、权限和资源状态；审批、发布、任务和用户管理权限不得仅靠隐藏前端入口实现。
- 数据库结构变化使用可重复执行的 Alembic 迁移；不得手工改库绕过审批、发布或实验状态机，也不得用本地数据库覆盖其他环境。
- 周初、日报、周终、审批和实验任务必须幂等，失败、取消、重试和恢复保留审计且不得伪造业务结果。
- 外部网页、公告、研报和模型输出均视为不可信输入。密钥、密码、令牌、完整个人信息、内部堆栈和冗长模型内部推理不得进入前端、日志、截图、测试报告或 Git。
- 界面遵循“结果优先、证据按需、过程默认隐藏、决策始终可审计”；异步任务明确展示加载、失败、降级、低置信度和候选稀缺状态。

## 6. 验证与验收

按变更风险执行并如实记录：

1. 核对最新需求、正式规则、暂缓范围和验收条件。
2. 检查实际差异，确认无无关重构、意外接口、数据或依赖变化。
3. 执行适用的格式化、静态检查、类型检查、针对性测试、完整测试和构建；命令以根 README 与实际配置为准。
4. 重点覆盖规则优先级、0～5 只容量、交易日与复权边界、停牌/涨跌停、数据降级、名单冻结、权限、幂等和未来数据泄漏。
5. 涉及数据库时验证迁移、事务、重复执行和必要的回退路径；涉及任务时验证失败、恢复、取消和审计。
6. 前端变化必须在真实浏览器检查桌面与移动视口、正常与异常状态、权限路径及关键交互，不能只凭 DOM、接口成功或单一截图判定通过。
7. 失败的检查不得跳过后宣告完成；最终明确说明实际改动、已执行与未执行验证、失败项、运行方式、未完成项和已知风险。

## 7. Git、发布与跨任务同步

- 保持改动小而聚焦；修复缺陷时增加相应回归测试，核心行为变化时同步更新正式文档。
- 未经用户明确要求，不创建分支、提交、推送、发布或部署。推送、CI 和生产部署是相互独立的状态，不得混称。
- 当前没有正式生产发布流程。云端发布前必须另行确认唯一发布入口，并验证配置、HTTPS、认证、任务恢复、日志监控、备份、恢复、回滚和数据源稳定性；本机可运行不等于生产就绪。
- 同一工作区的文件会共享，但不同任务的上下文和完成状态不会自动同步。任务结束时以 Git、数据库审计、核心文档和实际健康检查为事实来源，并区分文件改动、测试、提交、远端、数据库和部署状态。
